# # This is a basic workflow to help you get started with Actions

# name: Aibit Deployment

# # Controls when the action will run.
# on:
#   # Triggers the workflow on push or pull request events but only for the master branch
#   push:
#     branches: [main]

# # A workflow run is made up of one or more jobs that can run sequentially or in parallel
# jobs:
#   # This workflow contains a single job called "build"
#   deploy:
#     # The type of runner that the job will run on
#     runs-on: ubuntu-latest

#     # Steps represent a sequence of tasks that will be executed as part of the job
#     steps:
#       - name: deploy on production
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.HOST }}
#           username: ${{ secrets.USERNAME }}
#           key: ${{ secrets.PRIVATE_KEY }}
#           port: 22
#           script: |
#             cd /root/translator/aibit/aibit-backend
#             git pull origin main
#             npm install --production
#             npm run build
#             docker compose up --build -d app_2
#             DOMAIN=http://127.0.0.1:3001 node scripts/wait-on-deploy.js
#             docker compose up --build -d app_1
#             DOMAIN=http://127.0.0.1:3002 node scripts/wait-on-deploy.js
#           script_stop: true
